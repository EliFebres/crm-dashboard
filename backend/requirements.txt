# Runtime dependencies, pinned exactly. The compatible ranges live in pyproject.toml.
#
# `crm_sync` itself still imports only the standard library — sqlite3, json, pathlib,
# urllib, dataclasses, logging, hashlib, uuid. What changed is the distribution:
# portfolio_data's DataFrame layer (portfolio_data/frames.py) is the documented way in and
# out, and the notebooks under backend/notebooks/ need a server to open in. So "no third-
# party dependencies" is no longer true of `pip install -e backend`, and saying otherwise
# would leave a user with an ImportError and no explanation.
#
#   pip install -e backend     # installs these, and puts crm_sync / portfolio_data on sys.path
#
# pandas 3.0 requires Python >= 3.11 and requires-python here is >= 3.9, so the marker keeps
# an older interpreter on the last 2.x release that supports it. Both pins resolve.
pandas==3.0.5 ; python_version >= "3.11"
pandas==2.3.3 ; python_version <  "3.11"

# Only needed to open the notebooks. Nothing under backend/ imports it, so a scheduled job
# that only calls create_client_engagement can drop this line.
jupyterlab==4.6.2

# What you actually need to run this:
#
#   1. Python >= 3.9                     (see .python-version; developed on 3.14)
#   2. pip install -e backend            (see pyproject.toml — puts `crm_sync` and
#                                         `portfolio_data` on sys.path so the imports work
#                                         from any directory)
#   3. SQLITE_DIR=/path/to/db            (the folder holding engagements.sqlite and
#                                         users.sqlite — the same one the Next.js app uses)
#
# SQLITE_DIR is read from the environment, or from a .env file beside this folder or at the
# repo root. In a checkout it is already configured; nothing to export by hand.
#
# If you add a dependency, pin it exactly (package==1.2.3) and say why in a comment.
