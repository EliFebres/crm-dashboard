"""
Foundation types: the data shapes that cross the crm_sync boundary, and the exception
hierarchy raised across it.

Nothing in here imports from the rest of the package, so every other subpackage can
depend on it freely.
"""
