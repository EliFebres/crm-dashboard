"""
The pre-write matrix, as pure functions over payloads.

`vocabulary.py` holds the controlled lists — sleeves, breakdown dimensions and their
buckets, market series and their tenors — and is the file to edit when the dashboard grows
a card. `rules.py` holds the checks, each one traceable to a specific silent failure it
prevents.

Importable, but not the supported surface — see the package docstring.
"""
